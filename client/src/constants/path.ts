const path = {
  home: '/',
  login: '/login',
  register: '/register',
  user: '/user',
  profile: '/user/profile',
  changePassword: '/user/password',
  logout: '/logout'
} as const

export default path
